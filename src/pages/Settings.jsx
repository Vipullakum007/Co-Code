import React from 'react';

const Settings = () => {
    return (
        <div className='settings-container'>
            <h2>Settings</h2>
            <hr />
            <div className="settings-block">
                {/* Add font, theme settings, etc. */}
            </div>
        </div>
    );
};

export default Settings;
